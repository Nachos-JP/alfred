{
  "name": "gekko",
  "version": "0.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron src/",
    "dev": "node-sass src/style/scss --output src/style --output-style expanded && electron src/",
    "package:linux": "electron-packager src/ --overwrite --platform=linux --arch=x64 --out=dist",
    "package:mac": "electron-packager src/ --overwrite --platform=darwin --arch=x64 --out=dist",
    "package:win": "electron-packager src/ --overwrite --platform=win32 --arch=x64 --out=dist",
    "package:all": "electron-packager src/ --overwrite --platform=all --arch=x64 --out=dist",
    "sass": "node-sass -w ./style/scss --output ./style --output-style expanded &"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nachos-JP/gekko.git"
  },
  "author": "Shota Maekawa",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Nachos-JP/gekko/issues"
  },
  "homepage": "https://github.com/Nachos-JP/gekko#readme",
  "devDependencies": {
    "electron-packager": "^13.1.0",
    "node-sass": "^4.11.0"
  },
  "dependencies": {
    "electron": "^4.0.6",
    "electron-store": "^2.0.0",
    "vue": "^2.6.7"
  },
  "private": true
}
